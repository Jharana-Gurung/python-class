def swap_pairs(lst):
    i = 0
    while i < len(lst) - 1:

        temp = lst[i]
        lst[i] = lst[i + 1]
        lst[i + 1] = temp

        i = i + 2
    return lst


print(swap_pairs([1, 2, 3, 4]))
print(swap_pairs([1, 2, 3]))
